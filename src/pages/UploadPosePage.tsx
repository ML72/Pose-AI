import CustomPage from '../components/CustomPage';

const UploadPosePage = () => {
  return (
    <CustomPage>
      {/* Upload or capture pose image UI will go here */}
      <h1>Upload or Capture Pose</h1>
    </CustomPage>
  );
};

export default UploadPosePage;
